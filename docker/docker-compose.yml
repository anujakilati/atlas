version: '3.8'
services:
  app:
    build: ./docker
    volumes:
      - ./:/app
    ports:
      - "8000:8000"
    environment:
      - REDIS_URL=redis://redis:6379/0
    depends_on:
      - redis
  redis:
    image: redis:7
    command: ["redis-server", "--save", "", "--appendonly", "no"]
