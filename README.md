High-performance AI CCTV analysis — minimal reference implementation

This repository contains a modular, local-first CCTV analysis pipeline using YOLO, OpenCV, FFmpeg, FastAPI and an optional local VLM (Nemotron/ollama/vLLM).

See `docker/` for container setup and `backend/` for the core implementation.
